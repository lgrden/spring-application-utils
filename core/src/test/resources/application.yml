spring:
  main:
    banner-mode: off
  application:
    name: spring-application-utils-core

server:
  address: localhost
  port: 8080

messages:
  default-locale: en
  base-names:
    - msg/test1

test:
  system:
    build:
      version: 1.0.0
      buildDate: today
      gitCommit: ABCD
      gitTimeStamp: 2022-01-01 12:34:27
      gitBranch: main
  slack:
    hook:
      type: test
      enabled: true
      hook: https://hooks.slack.com/services/Test
      channel: test-channel
      username: test-username
      icon_emoji: ":robot_face:"